config BR2_TPLINK_SDK
	bool "SDK"
	help
		Enable this option if you want to build SDK in your switch system

config BR2_TPLINK_SDK_DBG
	bool "SDK CLI Debug"
	help
		Enable SDK CLI application or process (for debugging)
